build/
build-*/
build_*/
# The editor's private state: scratch, recovery, and the delete archive.
# Ignored wholesale so a future subdirectory cannot leak into git status.
.ssg/

# Runtime test artifacts (created under CWD when test binaries run outside build/)
runtime_editing/
runtime_language/
runtime_navigation/
runtime_presentation/
runtime_files/
runtime_snapshot/
runtime_snapshot_*/
hit_test_root/
