backlog.md
build/
build-*/
build_*/
# The editor's private state: scratch, recovery, and the delete archive.
# Ignored wholesale so a future subdirectory cannot leak into git status.
.ssg/

# Runtime test artifacts.
/.test-runtime/
# Stale help exports a test used to drop in the repo root (kept ignored so such
# an artifact can never be accidentally committed even if one reappears).
/help.md
/help.txt

# Working artifacts from agent review and analysis passes. These are inputs to a
# change, not documents of record: code-review.md alone churned 528 lines across
# two accidental commits before being ignored (doc/agility-plan.md).
code-review.md
code-review-*.md
agility-analysis-*.md
tasks.md
