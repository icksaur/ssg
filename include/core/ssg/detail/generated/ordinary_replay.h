// Generated by protocol/schema/generate_semantic_wire.mjs.
// Source: protocol/schema/semantic_wire.mjs. Do not edit.
#pragma once

#include <optional>

namespace ssg::detail::generated {

template <typename Delta>
decltype(auto) ordinaryReplacement(const Delta& delta) {
    if constexpr (requires { delta.replacement; }) {
        return (delta.replacement);
    } else {
        return (delta);
    }
}

// CONTRACT: This is the sole ordinary-section replay inventory. It returns a
// candidate and never mutates the retained base.
template <typename Sections, typename Delta>
std::optional<Sections> replayOrdinarySessionSections(
    const Sections& base, const Delta& delta) {
    auto next = base;
    {
        const auto& change = delta.selection();
        if (change.changed != change.replacement.has_value()) return std::nullopt;
        if (change.replacement) next.selection = *change.replacement;
    }
    {
        const auto& change = delta.history();
        if (change.changed != change.replacement.has_value()) return std::nullopt;
        if (change.replacement) next.history = *change.replacement;
    }
    {
        const auto& change = delta.clipboard();
        if (change.changed != change.replacement.has_value()) return std::nullopt;
        if (change.replacement) next.clipboard = *change.replacement;
    }
    {
        const auto& change = delta.promptStatus();
        if (change.changed != change.replacement.has_value()) return std::nullopt;
        if (change.replacement) next.promptStatus = *change.replacement;
    }
    {
        const auto& replacement = ordinaryReplacement(delta.search());
        if (replacement) next.search = *replacement;
    }
    {
        const auto& replacement = ordinaryReplacement(delta.findReplace());
        if (replacement) next.findReplace = *replacement;
    }
    {
        const auto& change = delta.keymap();
        if (change.changed != change.replacement.has_value()) return std::nullopt;
        if (change.replacement) next.keymap = *change.replacement;
    }
    {
        const auto& replacement = ordinaryReplacement(delta.textEncoding());
        if (replacement) next.textEncoding = *replacement;
    }
    {
        const auto& replacement = ordinaryReplacement(delta.tabs());
        if (replacement) next.tabs = *replacement;
    }
    {
        const auto& replacement = ordinaryReplacement(delta.followEdits());
        if (replacement) next.followEdits = *replacement;
    }
    {
        const auto& replacement = ordinaryReplacement(delta.syntax());
        if (replacement) next.syntax = *replacement;
    }
    {
        const auto& replacement = ordinaryReplacement(delta.lspSync());
        if (replacement) next.lspSync = *replacement;
    }
    {
        const auto& replacement = ordinaryReplacement(delta.lspFeatures());
        if (replacement) next.lspFeatures = *replacement;
    }
    {
        const auto& replacement = ordinaryReplacement(delta.theme());
        if (replacement) next.theme = *replacement;
    }
    {
        const auto& replacement = ordinaryReplacement(delta.palette());
        if (replacement) next.palette = *replacement;
    }
    {
        const auto& change = delta.noticeView();
        if (!change.changed && change.replacement) return std::nullopt;
        if (change.changed) next.noticeView = change.replacement;
    }
    {
        const auto& replacement = ordinaryReplacement(delta.watcherAvailable());
        if (replacement) next.watcherAvailable = *replacement;
    }
    return next;
}

}  // namespace ssg::detail::generated
